  {container_name}:  
    image: {image}
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE={name}
    volumes:
      - ./{name}/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - '{port}'