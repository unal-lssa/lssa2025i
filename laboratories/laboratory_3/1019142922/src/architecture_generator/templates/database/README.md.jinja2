# {{ component.name }} Database

This is a MySQL database component for the {{ component.name }} service.

## Configuration

- Database Name: {{ component.name }}
- Port: 3306
- Root Password: root

## Initialization

The database is initialized using the `init.sql` script when the container starts. 